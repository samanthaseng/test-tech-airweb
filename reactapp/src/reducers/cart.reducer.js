const cartReducer = function (cart = [], action) { 
    if (action.type === 'add') {
      

    } else if (action.type === 'delete') {
        
    } else {
      return cart;
    }
};

export default cartReducer;